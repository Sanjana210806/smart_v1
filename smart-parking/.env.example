# Copy this file to ".env.local" and adjust if needed (do not commit .env.local).
# Local dev: if unset, the app defaults to http://localhost:8080 for API calls.

# VITE_API_BASE_URL=http://localhost:8080
